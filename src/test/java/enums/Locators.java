package enums;

/**
 * enums -  This contains enums for all of the valid locators that we use
 */

public enum Locators {
    ClassName, CssSelector, Id, LinkText, Name, PartialLink, TagName, Xpath
}
